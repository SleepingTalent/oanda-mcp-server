services:
  oanda-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${MCP_PORT:-8000}:${MCP_PORT:-8000}"
    env_file:
      - .env
    environment:
      - MCP_TRANSPORT=http
    restart: unless-stopped
